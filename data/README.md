# Data directory policy

This directory contains documentation and, in the future, synthetic or anonymized sample data only.

Real Human OS RAW data must not be committed to this public repository.

Use local ignored directories such as `raw/`, `private/` or `exports/` for real personal data during development.
